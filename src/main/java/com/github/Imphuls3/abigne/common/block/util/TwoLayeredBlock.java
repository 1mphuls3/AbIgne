package com.github.Imphuls3.abigne.common.block.util;

import net.minecraft.world.level.block.Block;

public class TwoLayeredBlock extends Block {
    /**
     * Empty block class - only used for block model data gen to decide whether to use a normal block model or a two layered block model
     * */
    public TwoLayeredBlock(Properties properties) {
        super(properties);
    }
}
